# 电影投票网站

一个纯前端的电影投票 Demo，包含：

- 多选投票（本周最想看）；
- 每部电影独立评分（1-10 分）；
- 每部电影评论；
- 投票与评分数据使用浏览器 `localStorage` 持久化。

## 使用方式

直接打开 `index.html`，或者在仓库目录启动静态服务：

```bash
python3 -m http.server 8000
```

然后访问 <http://localhost:8000>。
